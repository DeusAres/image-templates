import funPIL as df
from resources.paths import *

def main(W: int=1000, 
            badge: list[str]="build",
            result: list[str]="passing",
            color: list[str]='#292929') -> df.Image:
    pass

    fontPath = str(FONTS / "Consolas.ttf")